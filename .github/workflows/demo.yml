name: Hello World!

on:
  push:
  pull_request:

jobs:
  linux:
    runs-on: ubuntu-latest
    name: Linux
    steps:
      - uses: actions/checkout@v3
        run: make hello.c
        run: ./hello.o

  macos:
    runs-on: macos-latest
    name: Macos
    steps:
      - uses: actions/checkout@v3
        run: gcc hello.c
        run: ./hello.o
